[project]
name = "followers-marble-race-engine"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "pymunk>=6.6",
    "Pillow>=10.0",
]

[project.optional-dependencies]
dev = ["pytest>=7.0", "pytest-cov>=4.0"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
